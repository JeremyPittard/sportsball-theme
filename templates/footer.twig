<footer class="bg-brand-secondary">
	{% if options.footer_message %}
		<div class="w-full bg-brand-primary text-brand-secondary">
			<div class="container flex items-center justify-between px-6 py-4 mx-auto">
				<div class="flex">
					<p class="mx-3">{{options.footer_message}}</p>
				</div>
			</div>
		</div>
	{% endif %}
	<div class="px-4 py-12 mx-auto max-w-7xl sm:px-6 lg:px-16">
		<div class="flex flex-col items-start justify-between pt-16 pb-6 gap-y-12 lg:flex-row lg:items-center lg:py-16">
			<div>
				<div class="flex items-center text-brand-primary">
					<div>
						{% if options.club_name %}
							<p class="font-semibold leading-6 text-brand-primary uppercase">
								{{options.club_name}}
							</p>
						{% endif %}
						{% if options.club_motto %}
							<p class="mt-1 text-sm">{{options.club_motto}}</p>
						{% endif %}
						{% if options.address %}
							{% set address = options.address|replace({' ': '+' }) %}
							<a href="http://maps.google.com/?q={{address}}" class="relative -my-2 -mx-3 rounded-lg px-3 py-2 text-sm text-brand-primary hover:text-brand-primary-900 transition-colors delay-150 hover:delay-[0ms]" target="_blank">
								<span class="relative z-10">come find us at
									<br>
									{{options.address}}
								</span>
							</a>
						{% endif %}
						<div class="flex flex-wrap mt-4">
							{% if options.facebook_link %}
								<a href="{{options.facebook_link}}" rel="noopener noreferrer" target="_blank" class="p-2">
									<img src="{{ theme.link }}/img/fb.png" alt="link to our facebook page" class="w-6"/>
								</a>
							{% endif %}
							{% if options.twitter_link %}
								<a href="{{options.twitter_link}}" rel="noopener noreferrer" target="_blank" class="p-2">
									<img src="{{ theme.link }}/img/twit.png" alt="link to our twitter page" class="w-6"/>
								</a>
							{% endif %}
							{% if options.instagram_link %}
								<a href="{{options.instagram_link}}" rel="noopener noreferrer" target="_blank" class="p-2">
									<img src="{{ theme.link }}/img/insta.png" alt="link to our instagram page" class="w-6"/>
								</a>
							{% endif %}
							{% if options.tiktok_link %}
								<a href="{{options.tiktok_link}}" rel="noopener noreferrer" target="_blank" class="p-2">
									<img src="{{ theme.link }}/img/tt.png" alt="link to our tiktok page" class="w-6"/>
								</a>
							{% endif %}
						</div>
					</div>
				</div>
				{% if footer_menu %}
					<nav class="flex gap-8 mt-11">
						{% for item in items %}
							{% if item.icon %}
								<a href="{{item.url}}" class="relative -my-2 -mx-3 rounded-lg px-3 py-2 text-sm text-brand-primary hover:text-brand-primary-900 transition-colors delay-150 hover:delay-[0ms]" target="{{item.target}}">
									<img src={{Image(item.icon).src}} alt="item.title" class="max-w-[180px]"/>
								</a>
							{% else %}
								<a href="{{item.url}}" class="relative -my-2 -mx-3 rounded-lg px-3 py-2 text-sm text-brand-primary hover:text-brand-primary-900 transition-colors delay-150 hover:delay-[0ms]" target="{{item.target}}">
									<span class="relative z-10">{{item.title}}</span>
								</a>
							{% endif %}
						{% endfor %}
					</nav>
				{% endif %}
			</div>
			<div class="relative flex items-center self-stretch p-4 -mx-4 transition-colors group hover:bg-brand-secondary-600 sm:self-auto sm:rounded-2xl lg:mx-0 lg:self-auto lg:p-6">
				<div class="relative flex items-center justify-center flex-none w-36 h-36 bg-brand-secondary rounded-xl">
					{% if options.logo %}
						<a href="/">
							<img src="{{options.logo.url}}" alt="{{options.club_name}} Logo" class="w-full"/>
						</a>
					{% endif %}

				</div>
			</div>

		</div>
		<div class="flex flex-col items-center pt-8 pb-12 border-t border-brand-primary-50 md:flex-row-reverse md:justify-between md:pt-6">
			<div class="max-w-xl">
				{% if options.subscribe_link %}
					<form class="flex flex-col items-center justify-center max-w-sm mx-auto" action="">
						<div class="flex flex-col w-full gap-1 mt-3 sm:flex-row">
							<input name="email" type="email" class="block w-full px-4 py-2 text-sm font-medium text-gray-800 placeholder-gray-400 bg-white border border-gray-300 rounded-full font-spline focus:border-brand-primary focus:outline-none focus:ring-2 focus:ring-brand-primary/50 disabled:opacity-50" placeholder="Enter your email..." required=""><button type="button" class="items-center inline-flex  justify-center w-full px-6 py-2.5 text-center text-white duration-200 bg-black border-2 border-black rounded-full nline-flex hover:bg-transparent hover:border-black hover:text-brand-primary focus:outline-none lg:w-auto focus-visible:outline-black text-sm focus-visible:ring-black">
								<div style="position: relative"></div>
								Subscribe
								<!-- -->
								<svg xmlns="http://www.w3.org/2000/svg" viewbox="0 0 20 20" fill="currentColor" aria-hidden="true" class="w-4 h-auto ml-2">
									<path fill-rule="evenodd" d="M3 10a.75.75 0 01.75-.75h10.638L10.23 5.29a.75.75 0 111.04-1.08l5.5 5.25a.75.75 0 010 1.08l-5.5 5.25a.75.75 0 11-1.04-1.08l4.158-3.96H3.75A.75.75 0 013 10z" clip-rule="evenodd"></path>
								</svg>
							</button>
						</div>
					</form>
				{% endif %}
				<p class="mt-6 text-sm text-brand-primary md:mt-0">
					site made by
					<a href="http://jpittard.net" target="_blank" class="hover:underline transition-colors delay-150 hover:delay-[0ms]" rel="noopener noreferrer">this guy.</a>
				</p>
			</div>
			<p class="mt-6 text-sm text-brand-primary md:mt-0">
				© Copyright
				<!-- -->
				{{"now"|date('Y')}}
				<!-- -->
				All rights reserved.
			</p>
		</div>
	</div>
</footer>
